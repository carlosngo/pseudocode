//correct any minor syntax inconsistencies when necessary (refer to pseudocode's language design)

func void testOne() {
	int x = 0;
	scan("Hello. I need your number: " ,x);

	for int i to 10 { //syntax error #1 missing assignment operator
		print("Petmalu " +x+); //syntax error #2 additional + sign at end of print

	//syntax error #3 missing curly brace
}

func void testTwo(float x) {
	if(x = 4) then //syntax error #4 expected comparison operator
	{
		print(x is a very huge number); //syntax error #5 expected double quotes
	}
}

main() {
	float value = (5 * 1) + (5 * 5 / 1 + 3 + (4 + (5 * 3)))/2.0f * 8.0f;

	testOne()(x); //syntax error #6 redundant parenthesis
	testTwo(123 ++value); //syntax error #7 duplicate plus sign
}
