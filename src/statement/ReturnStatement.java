package statement;

import manager.ExecutionManager;

public class ReturnStatement extends Statement {
    public ReturnStatement(ExecutionManager executionManager) {
        super(executionManager);
    }
}
